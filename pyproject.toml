[project]
name = "cogito-review"
version = "0.0.1"
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["shared", "backend", "agent"]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
